#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

export DH_OPTIONS
export DH_GOPKG := github.com/shell909090/goproxy
export GOPATH := $(shell realpath `pwd`"/../../../.."):/usr/share/gocode

%:
	dh $@
 # --buildsystem=golang --with=golang
